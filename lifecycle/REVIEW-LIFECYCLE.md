# Final lifecycle derivative and one-job hook review

Review complete executable composition and launch gates through P2. Prior bundle455a4678866519babf4af3f1dfec48024c49686119566f5f913d5a8875f43098 remains immutable externally; new bundle is sealed independently. The61 retained controls passed plus12 affected actual PowerShell mocked controls; original functions failed11/12, including normal Kill(true), installer timeout and unbounded joins. Mocked controls are not Windows runtime proof. Native same-job parse/parameter binding/helper compilation and own smoke still mandatory.

Only one new disposable Windows job and at most one unchanged released2026.9.5 driver diagnostic is authorized after this review. No historical retry, pin weakening, product-source mutation, peer change or native acceptance promotion. Fresh host contract requires ImageOS win25-vs2026, ImageVersion20260907.229.1, OS10.0.26100, PowerShell7.6.5 exact digest, pinned Python3.13.7, own complete smoke under SAME bundle/host/runtime/run/attempt1/workflowSHA. Candidate is separately checked out clean393c80255dea6605a5665c6e0836d01fb6d18304/tree d4bb6a0016a6aab695c9c6fe67b40c76f3d9ee6b. Node24.20.0 toolcache executable pinned by official SHA256 and native ABI137.

New precise authority permits normal controlled shutdown only of SAME disposable job's newly launched owned foreground baseline Gateway BEFORE arming/updater. Cooperative route if applicable; otherwise root-only held-handle termination allowed, never process-tree/PID/name/port-only target termination. Original PID/creation/executable/hash/profile/root, same Process/SafeHandle, listener and descendant custody guards precede action; bounded terminal settlement before arming. Failure retains root without fallback action. Released internal hosted-stop capability exists, but public CLI uses supervised-service/PID/port discovery, not retained-handle routing; system-agent-engine capability requires unapproved inference, so root-only fallback chosen. LIFECYCLE-CONTRACT.json binds exact released source evidence. --stack-size=8192 is the release's own win32 default startup flag supplied up front to prevent its root respawn, not a released source patch. All unknown descendants fail admission.

Installer/command timeout may never terminate any child. Finite1200s wrapper wait and bounded0 terminal query, preserved original error and UNSETTLED custody. No Gateway/observer/update action in finally, including after arm. Normal fixture stop10s maximum, no retry. It is NOT proof of graceful service shutdown or update recovery.

Observer/fixture/query-only source remains immutable24ab. Product update argv1200/aggregate3600/60samples60s/caps64MiB primary64MiB compressed512MiB logical unchanged. Lossless gaps are retained. Prior successful smoke35596476995 is disposed and rejected as current proof. Existing workflow other jobs preserved; new selector excludes them, validates exclusive inputs and runs one diagnostic job. Review actual end-to-end: fresh smoke before product setup, release archive and installed bytes before execution, root lifecycle completion before arm, evidence uploaded on failures, no automatic retry.

NativeNEEDS_WORK; historical35529432478 holderUNKNOWN; installed2026.9.5 transaction.complete directly awaits own discardPackageUpdateBackup/rollback ownership gapUNFIXED. Observation cannot fix that dependency. Root retained/uploaded for named investigation; VM disposal distinct from process settlement. Runtime cleanup is conservative and source custody retained. No credentials intended; test token is generated in disposable profile and is not an operator credential.

## First review disposition and final repair

Accepted P2 descendant gap: initial ParentProcessId checks missed orphaned grandchildren. New held-job.cs creates only this baseline suspended, assigns its original CreateProcess handle to an unnamed accounting-only job BEFORE ResumeThread, with default no-breakaway behavior and NO kill-on-close/termination limit. Counts TotalProcesses=1 and ActiveProcesses=1 required before action; TotalProcesses=1/ActiveProcesses=0 required after root-only action. Any descendant ever present (including raced or already-exited descendants) prevents observation. Job assignment/resume failure retains the original suspended handle/resources; never kill/erase on failure. Native accounting/launch remains unverified until the ONE job. Three added controls failed against previous source and pass repaired.

The test_product.py:25 finding is not reproduced: Session.finish in run.py lines151-166 unconditionally calls process_identity for every retained child (including poll=None), then returns false on unsettled state; complete_diagnostic raises expected ValueError. Exact targeted test_product.ProductCompletion.test_unsettled_and_late_exit PASSES unchanged, as do61 retained controls. Chunk4 had test source without its unchanged run.py dependency. Please adjudicate against the supplied actual run.py, not an assumed early return; no source/test change was justified by that finding.
